# CONTEXT: Anime Picks

เว็บหน้าเดียวแนะนำอะนิเมะ ภาษาไทย สร้างโดย Tree ร่วมกับ Claude Code

## เป้าหมาย
เว็บหน้าเดียว (single page) ที่คัดอะนิเมะแนะนำ ให้ผู้ใช้ค้นหา เลือกดูตามแนว สุ่มเรื่องแนะนำ เก็บเรื่องที่ชอบ และไปดูตัวอย่างได้

## ไฟล์
| ไฟล์ | หน้าที่ |
|---|---|
| `anime/index.html` | ต้นฉบับ HTML + CSS + JS ในไฟล์เดียว เปิดในเบราว์เซอร์ได้เลย |
| `anime/anime-picks.html` | เวอร์ชันสำหรับเผยแพร่เป็น Artifact สร้างจาก `index.html` โดยตัดแท็ก `<html>` `<head>` `<body>` และ `<meta>` ออก ตั้ง `<title>` เป็น "Anime Picks" และเพิ่ม `color-scheme: dark` |
| `task.md` | รายการงานที่เสร็จและยังค้าง |
| `.claude/settings.json` | hook ของโปรเจกต์ (ดูด้านล่าง) |
| `.agents/skills/` | skill `grill-with-docs` และ `grilling` จาก `mattpocock/skills` |

Artifact ที่เผยแพร่ (ส่วนตัว): https://claude.ai/artifact/Gdi9RNbSrGPyGvevHm4s2k (เวอร์ชัน 3)

## ข้อตกลงด้านดีไซน์
- ธีมมืดโทนน้ำเงินเข้ม สีเน้นเขียวมิ้นต์ `#34d399` ไล่ฟ้า `#38bdf8` (Tree เลือกโทนเขียว/ฟ้า) สีหัวใจ `#fb7185`
- ฟอนต์ Noto Sans Thai (Google Fonts)
- Responsive: มือถือ 1 คอลัมน์ เดสก์ท็อปเป็น grid
- โปสเตอร์เป็นบล็อกไล่สีกับอีโมจิ ไม่ฝังรูปลิขสิทธิ์
- ข้อมูลทั้งหมดอยู่ใน array `animeList` ในสคริปต์ที่เดียว

## ฟีเจอร์
- **ปุ่มสุ่ม** "🎲 สุ่มเรื่องแนะนำวันนี้" ใน Hero กดแล้วสุ่มใหม่ทุกครั้ง ไม่ซ้ำเรื่องล่าสุด และเปิด modal ของเรื่องนั้น
- **Favorite** ปุ่มหัวใจบนการ์ดทุกใบและใน modal เก็บใน `localStorage` คีย์ `animePicks.favorites` (เก็บเป็นรายการชื่อเรื่อง) ห่อด้วย try/catch
- **ค้นหา** ช่องค้นหาเหนือปุ่มกรอง ค้นจากชื่อเรื่อง แนว ปี เรื่องย่อ และเหตุผลที่แนะนำ ไม่สนตัวพิมพ์ พิมพ์หลายคำได้ (ต้องตรงทุกคำ) กด Esc ในช่องเพื่อล้างคำค้น แสดงจำนวน "พบ n จาก 16 เรื่อง" และข้อความเมื่อไม่พบผล
- **ตัวกรอง** ตามแนว และ "❤ ที่ชอบ (n)" ต่อท้าย ทำงานร่วมกับช่องค้นหา (ผลลัพธ์ต้องตรงทั้งคำค้นและตัวกรอง) ถ้ายังไม่มีเรื่องที่ชอบให้แสดงข้อความแนะนำ
- **ดูตัวอย่าง** ปุ่มใน modal ลิงก์ค้นหา YouTube "ชื่อเรื่อง official trailer" เปิดแท็บใหม่ (`rel="noopener noreferrer"`)
- **รายการ** 16 เรื่อง

## สถานะข้อมูล
- ปีที่ฉายของ 8 เรื่องที่เพิ่มทีหลัง (Cowboy Bebop, Death Note, One Punch Man, Violet Evergarden, Jujutsu Kaisen, Spirited Away, Haikyu!!, Bocchi the Rock!) ตรวจกับแหล่งบนเว็บแล้ว ตรงทั้งหมด
- คะแนนที่ตรวจแล้วกับ MyAnimeList: Cowboy Bebop 8.8, Death Note 8.6, One Punch Man 8.5, Violet Evergarden 8.6, Spirited Away 8.8, Haikyu!! 8.4
- คะแนนที่ยังไม่ได้ตรวจ: Jujutsu Kaisen, Bocchi the Rock! และ 8 เรื่องแรก (เขียนจากความจำ)
- ข้อความท้ายหน้ายังเป็น "คะแนนเป็นความเห็นส่วนตัว" ทั้งที่คะแนนบางส่วนอ้างอิง MyAnimeList (Tree ปฏิเสธการแก้เป็น "คะแนนโดยประมาณ" ยังรอตัดสินใจ)

## ข้อจำกัดที่รู้อยู่แล้ว
- favorite ไม่ข้ามเครื่อง และหายถ้าล้างข้อมูลเบราว์เซอร์
- ลิงก์ตัวอย่างเป็นผลค้นหา ไม่ใช่วิดีโอเฉพาะเรื่อง
- ยังไม่ได้ทดสอบการใช้งานจริงบนหน้าที่เผยแพร่ (ปุ่มสุ่ม หัวใจ ตัวกรอง ค้นหา จอแคบ และกรณี `localStorage` ใช้ไม่ได้) ตรวจได้แค่ syntax ของสคริปต์
- การทดสอบอัตโนมัติทำไม่ได้ในสภาพแวดล้อมนี้: Chrome แบบไม่มีหน้าต่างค้าง และการติดตั้ง jsdom ถูกยกเลิก

## Hooks (`.claude/settings.json`)
- `Stop` → `say -v Kanya "เสร็จแล้วเว้ย"` (async)
- `PermissionRequest` → `say -v Kanya "มากดอนุญาต"` (async)
- ต้องเปิด session จากโฟลเดอร์ `ClaudeCode` ถึงจะใช้ได้ ถ้าเสียงไม่ขึ้นให้เปิด `/hooks` หรือรีสตาร์ท

## วิธีอัปเดตเว็บ
1. แก้ `anime/index.html`
2. สร้าง `anime/anime-picks.html` ใหม่จาก `index.html` ด้วยขั้นตอนตัดแท็กข้างต้น
3. เผยแพร่ไฟล์เดิมซ้ำเพื่อคงลิงก์เดิม

## ความชอบของผู้ใช้
- ชื่อ Tree ใช้ภาษาไทยเป็นหลัก ปนอังกฤษได้ ตอบตามภาษาที่ใช้
- อย่าปิดหรือฆ่า Chrome ของผู้ใช้ (ใช้ `pkill` เฉพาะกระบวนการทดสอบเท่านั้น)
- เมื่อผู้ใช้ยกเลิกการเรียกเครื่องมือ (เช่น การแก้ไฟล์หรือติดตั้งแพ็กเกจ) ให้หยุดและรอคำสั่ง ไม่ลองทำซ้ำในรูปแบบอื่น
